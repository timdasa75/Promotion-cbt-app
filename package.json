{
  "name": "promotion-cbt-app",
  "version": "1.0.0",
  "description": "An interactive CBT practice application for Nigerian civil service promotion exams at the directorate level.",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js --reporter=line --max-failures=1",
    "test:smoke:full": "playwright test tests/smoke.spec.js",
    "test:unit": "node --test \"tests/unit/*.test.js\"",
    "verify:release": "python scripts/validate_taxonomy.py --strict-duplicates --strict-metadata --allow-missing-topic-files && npm run audit:topic-banks:ci && npm run audit:questions:strict && npm run test:unit && npm run build && npm run test:smoke",
    "verify:pages": "python scripts/validate_taxonomy.py --strict-duplicates --strict-metadata --allow-missing-topic-files && npm run audit:topic-banks:ci && npm run test:unit && npm run build",
    "audit:topic-banks": "node scripts/audit_topic_bank_contamination.mjs",
    "audit:topic-banks:strict": "node scripts/audit_topic_bank_contamination.mjs --fail-on-findings",
    "audit:topic-banks:ci": "node scripts/audit_topic_bank_contamination.mjs --fail-on-findings --allow-missing-topic-files",
    "audit:questions": "node scripts/audit_questions.mjs",
    "audit:questions:strict": "node scripts/audit_questions.mjs --fail-on-findings",
    "fix:questions": "node scripts/fix_question_quality.mjs",
    "check:worker-routes": "node scripts/check_worker_routes.mjs",
    "check:worker-routes:warn": "node scripts/check_worker_routes.mjs --warn",
    "check:worker-routes:json": "node scripts/check_worker_routes.mjs --json",
    "check:worker-routes:selar": "node scripts/check_worker_routes.mjs --check-selar-key",
    "check:worker-routes:flutterwave": "node scripts/check_worker_routes.mjs --check-flutterwave-key",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "antigravity:init": "antigravity init",
    "design:export": "antigravity export-design",
    "antigravity:verify": "antigravity verify",
    "antigravity:run": "antigravity run"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/timdasa75/Promotion-cbt-app.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/timdasa75/Promotion-cbt-app/issues"
  },
  "homepage": "https://github.com/timdasa75/Promotion-cbt-app#readme",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "jspdf": "^4.2.1",
    "vite": "^8.0.1",
    "vite-plugin-static-copy": "^3.3.0",
    "wrangler": "^4.86.0"
  },
  "overrides": {
    "undici": "^7.24.0",
    "ws": "^8.20.1",
    "anymatch": {
      "picomatch": "^2.3.2"
    },
    "readdirp": {
      "picomatch": "^2.3.2"
    }
  }
}
